import { AppMenuButton } from '@/components/app/AppMenuButton';
import { RateForm } from './RateForm';

export default function RatePage() {
  return <RateForm appMenuButton={<AppMenuButton />} />;
}
